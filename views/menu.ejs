<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<body>
  <h1>Full Menu</h1>
  <%- include('./partials/nav') %>
  <p><%= menuu.menu %></p>
  <ul>
    <!-- Add the index parameter -->
    <% menuu.forEach((menuu) => { %>
      <!-- Wrap each li in an <a> tag -->
        <li><%= menuu.name %>: price =<%= menuu.price %>$ : rating = <%= menuu.rating %>  
            <p>details: <%= menuu.details %> </p>
        </li>
    <% }); %>
  </ul>
</body>
</html>

